# Cycle 1 → Cycle 2 handoff — 2026-05-10 13:30 UTC

## Cycle 1 deliverables

### Paper (7 documents)

| Doc | Topic | Status |
|-----|-------|--------|
| 19  | N4 (no-waste invariant) | Plausibly true, COUPLED with N5 |
| 20  | N5 (P_ep amortization) | Joint constraint with N4 → rate-limited interleaving |
| 21  | Rate-limited scheduler design | Structure closes, absorber bottleneck |
| 22  | Strengthened absorber | CLOSES the gap (W(∞) = O(1) not divergent) |
| 23  | A1 (cellification ≥ 2 cells) | DISCHARGED via cut-thickness cap |
| 24  | A2 (initial-state thresholds) | T_0 = 16 computed; N=100 BSSF cert FAILS |
| 25  | Balanced cert construction | BalancedCertN22 satisfies ALL thresholds |

### Code (3 Lean files, all sorry-free)

| File | LoC | Function |
|------|-----|----------|
| NormalBoxFirstStep.lean | 198 | nbfStep: try normal-box, fall back to LRP cut |
| RateLimitedNbfStep.lean | 224 | rate-limited dispatching with absorber counter m |
| StrengthenedAbsorberStep.lean | 510 | strengthened absorber with classifyStrip + discardTotal |

### Certificates (2 new Lean certs)

| Cert | t | c | R | η | Status |
|------|---|---|---|---|--------|
| BalancedCertN22 | 23 | 9/20 | 8/3 | 0 | Sorry-free, satisfies all thresholds |
| BalancedCertN16 | 17 | varies | varies | 0 | Sorry-free, minimal companion |

## Critical gap remaining

The **Lean closure** still depends on `MeirMoser.AllStepsSucceedProof.balanced_c_share_positive_axiom`.

This axiom is what the cycle-1 paper work was designed to discharge. The Lean infrastructure (rate-limited + strengthened absorber files) exists and is sorry-free, but it's not yet WIRED into the closure path.

**The full closure proof requires** (cycle 2 work):
1. Prove `cumulative_lrp_cut_bound`: under `iteratedStrengthened`, the number of LRP cuts in [1, t] is O(t^{1−1/γ}). Uses `RateLimitedNbfStep`'s structural lemmas.
2. Prove `cumulative_discard_bound`: `discardTotal_t ≤ K · 1/√t` (from docs/22 analysis). Uses `StrengthenedAbsorberStep`'s structural lemmas.
3. Prove `p_ep_global_bound`: `P_ep(t) ≤ η` using the joint amortization from docs/22.
4. Combine to discharge `balanced_c_share_positive_axiom` as a theorem.
5. Update `BalancedCertN22.lean` to use the new theorem; verify `#print axioms` shows zero project axioms.

Estimated cycle-2 effort: 2-4 cycles of focused parallel work (paper proves the analytic bounds rigorously, code formalizes them in Lean).

## Cycle-2 priority for next run

### Priority 1 (paper-side): prove the cumulative LRP-cut bound rigorously

Output: `docs/26-cumulative-lrp-cut-proof.md`. Show that under rate-limited dispatching, the count `#LRP_cuts(t) ≤ K · t^{1−1/γ}` for some explicit K(γ, R, c). The argument:
- Each LRP cut consumes `m == k_target` of the counter cycle.
- So #LRP_cuts ≤ #(t/k_target) ≤ t/Nat.sqrt(t) = O(√t) for the simplified k_target.
- For γ = 4/3, k_target = ⌈t^{3/4}⌉, so #LRP_cuts ≤ t / t^{3/4} = O(t^{1/4}) = O(t^{1−1/γ}) ✓.

### Priority 2 (code-side): formalize the cumulative LRP-cut bound in Lean

Output: `lean/MeirMoser/CumulativeBound.lean`. Take the proved theorem `iteratedStrengthened_lrp_cuts_bound` showing the count of LRP cuts is bounded by a function of k. Use the structural lemmas from `StrengthenedAbsorberStep`.

### Both run in parallel via Task tool dispatch.

## Honest assessment

The framework's path to closure is now CONCRETE:
- Paper-side: rate-limited interleaving + strengthened absorber + cut-thickness cap.
- Code-side: 3 sorry-free files (~932 LoC) implementing the framework.
- Cert: BalancedCertN22 satisfies all thresholds.

What remains: prove the Lean analytic bounds and wire them into the closure. This is multi-cycle work but the path is precisely localized.

The framework MAY still fail at the analytic-bound level — even if the structural/discrete arguments close, the constants might not work out. Be honest if a cycle reveals this.

## Repo state at 2026-05-10 13:30 UTC

- HEAD: 4b2b5da (`feat(cert): BalancedCertN22 + N16`).
- Files added in cycle 1: ~10 docs + 3 Lean files + 2 certs + 2 results JSONs.
- Build green: `lake build` succeeds; only pre-existing warnings.
- `#print axioms BalancedCertN22.meir_moser_packs_unit_square`: depends on Lean core + `balanced_c_share_positive_axiom`.
